# local-telemetry-pipeline
This repository contains local testing scripts for processing mock telemetry data and normalizing raw tensors before feeding them into the model. Note: Large binary artifacts and dataset chunks are stored in Releases due to size constraints.
